import React , {
  Platform
} from 'react-native' ;

if(Platform.OS === 'android')
{
  module.exports = require('./AudioRecorder.android');
}
else if(Platform.OS === 'ios')
{
  module.exports = require('./AudioRecorder.ios');
}
